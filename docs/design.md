# alloc

enum 隐式的是常量表达式

Union 内的项目共享内存，如果条目不会同时使用，使用union是极好用的

静态成员变量不能在类内使用列表初始化

round 到8n的位运算，

free_list：静态数组，数组元素为 alloc::obj* 类型，用于存储不同大小空闲块的链表头指针。数组大小由 k_num_free_list_ 决定，初始时所有链表头指针都置为 0，表示链表为空

